/**
 * Future integration point for DocumentServer lifecycle:
 * - afterSave -> ON_SAVE
 * - afterPost -> ON_POST
 * - statusChanged -> ON_STATUS_CHANGE
 *
 * Stage 0 must not change document lifecycle behavior yet.
 */
export {};

